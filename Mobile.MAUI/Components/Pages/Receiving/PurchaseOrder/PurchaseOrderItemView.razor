@page "/receiving/purchaseorder/{OrderNumber}"

@using Mobile.MAUI.Components.Base
@using Shared.Libraries.ViewModel.PurchaseOrder
@using static Mobile.MAUI.Enums.CustomEnum
@using static MAUI.Helpers.FormatHelper

@layout CleanLayout
@inherits BaseComponent

<RadzenStack Gap="12px" Style="">

    <PageHeading Title="Purchase Order"
                 BackPath="/receiving"
                 Click="LoadPurchaseOrder"
                 Id="@OrderNumber" />

    <RadzenStack id="recent-scanned"
                 class="box-toolbar-page">
    </RadzenStack>

    <Loading IsLoading="ActionGetPOItems.Busy"
             Text="Loading PO Items...">

        <ChildContent>

            <RadzenStack>

                <RadzenTabs @bind-SelectedIndex="ActiveTabIndex">

                    <Tabs>

                        @* =========================
                           GOOD
                           ========================= *@

                        <RadzenTabsItem Text="Good">

                            <RadzenDataList TItem="PurchaseOrderLineVM"
                                            Data="GoodPOItems.Where(x => x.NSLineQuantityReceived != 0)"
                                            Style="margin-bottom:200px">

                                <Template Context="row">

                                    <RadzenStack Style="position:relative;overflow:clip !important;"
                                                 class="@(IsSelectedGood(row) ? "rz-p-2 selected-row" : "rz-p-2")"
                                                 Orientation="Orientation.Horizontal"
                                                 @onclick="@(() => SelectGoodLine(row))">

                                        <RadzenStack Gap="4px">

                                            <RadzenText Text="@row.MaterialName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.75rem" />

                                            <RadzenText Text="@row.VendorName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem" />

                                            <RadzenText Text="@($"{row.ScannedWeight}kg")"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem" />

                                        </RadzenStack>

                                        <RadzenStack Gap="0"
                                                     Style="position:absolute;right:0;top:0;width:max-content;height:100%;overflow:clip"
                                                     AlignItems="AlignItems.End">

                                            <RadzenStack Style="flex-grow:1"
                                                         class="@($"dynamic-qty-box {(row.AlreadyReceived ? "rz-background-color-success" : "rz-background-color-warning")}")">

                                                <RadzenText Text="@FormatQuantity(row.ScannedQuantity)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="@($"dynamic-qty-text {(row.AlreadyReceived ? "rz-color-white" : "")}")" />

                                            </RadzenStack>

                                            <RadzenStack Style="flex-grow:1;"
                                                         class="dynamic-qty-box rz-background-color-base-200">

                                                <RadzenText Text="@FormatQuantity(row.NSLineQuantityReceived)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="dynamic-qty-text" />

                                            </RadzenStack>

                                        </RadzenStack>

                                    </RadzenStack>

                                </Template>

                            </RadzenDataList>

                        </RadzenTabsItem>


                        @* =========================
                           BAD
                           ========================= *@

                        <RadzenTabsItem Text="Bad">

                            <RadzenDataList TItem="PurchaseOrderLineVM"
                                            Data="BadPOItems.Where(x => x.ScannedQuantity != 0)"
                                            Style="margin-bottom:200px">

                                <Template Context="row">

                                    <RadzenStack Style="position:relative;overflow:clip !important;"
                                                 class="@(IsSelectedBad(row) ? "rz-p-2 selected-row" : "rz-p-2")"
                                                 Orientation="Orientation.Horizontal"
                                                 @onclick="@(() => SelectBadLine(row))">

                                        <RadzenStack Gap="4px">

                                            <RadzenText Text="@row.MaterialName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.75rem" />

                                            <RadzenText Text="@row.VendorName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem" />

                                            <RadzenText Text="@($"{row.ScannedWeight}kg")"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem" />

                                        </RadzenStack>

                                        <RadzenStack Gap="0"
                                                     Style="position:absolute;right:0;top:0;width:max-content;height:100%;overflow:clip"
                                                     AlignItems="AlignItems.End">

                                            <RadzenStack Style="flex-grow:1"
                                                         class="@($"dynamic-qty-box {(row.AlreadyReceived ? "rz-background-color-success" : "rz-background-color-warning")}")">

                                                <RadzenText Text="@FormatQuantity(row.ScannedQuantity)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="@($"dynamic-qty-text {(row.AlreadyReceived ? "rz-color-white" : "")}")" />

                                            </RadzenStack>

                                            <RadzenStack Style="flex-grow:1;"
                                                         class="dynamic-qty-box rz-background-color-base-200">

                                                <RadzenText Text="@FormatQuantity(row.NSLineQuantityReceived)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="dynamic-qty-text" />

                                            </RadzenStack>

                                        </RadzenStack>

                                    </RadzenStack>

                                </Template>

                            </RadzenDataList>

                        </RadzenTabsItem>


                        @* =========================
                           MISSING
                           ========================= *@

                        <RadzenTabsItem Text="Missing">

                            <RadzenDataList TItem="PurchaseOrderLineVM"
                                            Data="MissingItems.Where(x => x.ScannedQuantity != 0)"
                                            Style="margin-bottom:200px">

                                <Template Context="row">

                                    <RadzenStack Style="position:relative;overflow:clip !important;"
                                                 class="@(IsSelectedMissing(row) ? "rz-p-2 selected-row" : "rz-p-2")"
                                                 Orientation="Orientation.Horizontal"
                                                 @onclick="@(() => SelectMissingLine(row))">

                                        <RadzenStack Gap="4px">

                                            <RadzenText Text="@row.MaterialName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.75rem" />

                                            <RadzenText Text="@row.VendorName"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem" />

                                            <RadzenText Text="Missing"
                                                        TextStyle="TextStyle.Subtitle2"
                                                        Style="font-size:.6rem;color:var(--rz-warning);" />

                                        </RadzenStack>

                                        <RadzenStack Gap="0"
                                                     Style="position:absolute;right:0;top:0;width:max-content;height:100%;overflow:clip"
                                                     AlignItems="AlignItems.End">

                                            <RadzenStack Style="flex-grow:1"
                                                         class="dynamic-qty-box rz-background-color-warning">

                                                <RadzenText Text="@FormatQuantity(row.ScannedQuantity)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="dynamic-qty-text" />

                                            </RadzenStack>

                                            <RadzenStack Style="flex-grow:1;"
                                                         class="dynamic-qty-box rz-background-color-base-200">

                                                <RadzenText Text="@FormatQuantity(row.NSLineQuantityReceived)"
                                                            Style="font-size:1.25rem;font-weight:500;"
                                                            class="dynamic-qty-text" />

                                            </RadzenStack>

                                        </RadzenStack>

                                    </RadzenStack>

                                </Template>

                            </RadzenDataList>

                        </RadzenTabsItem>

                    </Tabs>

                </RadzenTabs>


                @* =========================
                   SCAN ACTIONS
                   ========================= *@

                <RadzenCard class="floating-scan-actions rz-p-2 rz-border-radius-6">

                    <RadzenStack Gap="8px"
                                 Orientation="Orientation.Vertical">

                        <RadzenStack Orientation="Orientation.Horizontal"
                                     AlignItems="AlignItems.Center"
                                     JustifyContent="JustifyContent.SpaceBetween"
                                     Gap="8px">

                            @if (!IsActionPanelCollapsed)
                            {
                                <RadzenText TextStyle="TextStyle.Caption"
                                            class="rz-m-0">
                                    Scan Actions
                                </RadzenText>
                            }

                            <RadzenButton Icon="@(IsActionPanelCollapsed
                                ? "keyboard_arrow_up"
                                : "keyboard_arrow_down")"
                                          Size="ButtonSize.Small"
                                          ButtonStyle="ButtonStyle.Base"
                                          Variant="Variant.Flat"
                                          Click="@ToggleActionPanel" />

                        </RadzenStack>


                        @if (!IsActionPanelCollapsed)
                        {
                            <RadzenStack Gap="8px"
                                         Orientation="Orientation.Horizontal"
                                         AlignItems="AlignItems.Center">

                                @* GOOD / BAD / MISSING *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton Class="rz-border-radius-5"
                                                  Icon="@ScanStateIcon"
                                                  ButtonStyle="@ScanStateButtonStyle"
                                                  Size="ButtonSize.Small"
                                                  Click="@ToggleScanState"
                                                  Disabled="@ActionSaveScan.Busy" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                Class="scan-action-label rz-m-0">

                                        @ScanStateLabel

                                    </RadzenText>

                                </RadzenStack>


                                @* MOVE *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton class="rz-border-radius-5"
                                                  Icon="move_item"
                                                  ButtonStyle="@(MoveOn
                                                      ? ButtonStyle.Success
                                                      : ButtonStyle.Base)"
                                                  Size="ButtonSize.Small"
                                                  Click="@(args => ToggleMove())"
                                                  Disabled="@(ActionSaveScan.Busy
                                                      || NegateQuantity
                                                      || ScanState == ToggleState.Missing)" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                class="scan-action-label rz-m-0">

                                        Move

                                    </RadzenText>

                                </RadzenStack>


                                @* REMOVE *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton class="rz-border-radius-5"
                                                  Icon="remove"
                                                  ButtonStyle="@(NegateQuantity
                                                      ? ButtonStyle.Success
                                                      : ButtonStyle.Base)"
                                                  Size="ButtonSize.Small"
                                                  Click="@(args => ToggleNegateQuantity())"
                                                  Disabled="@(ActionSaveScan.Busy
                                                      || MoveOn
                                                      || ScanState == ToggleState.Missing)" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                class="scan-action-label rz-m-0">

                                        Remove

                                    </RadzenText>

                                </RadzenStack>


                                @* WEIGHT *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton class="rz-border-radius-5"
                                                  Icon="balance"
                                                  ButtonStyle="ButtonStyle.Base"
                                                  Size="ButtonSize.Small"
                                                  Click="@(args => ToggleWeight())"
                                                  Disabled="@(ActionSaveScan.Busy
                                                      || ScanState == ToggleState.Missing)" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                class="scan-action-label rz-m-0">

                                        Weight

                                    </RadzenText>

                                </RadzenStack>


                                @* MANUAL *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton class="rz-border-radius-5"
                                                  Icon="draft"
                                                  ButtonStyle="@(ManualEntry
                                                      ? ButtonStyle.Success
                                                      : ButtonStyle.Base)"
                                                  Size="ButtonSize.Small"
                                                  Click="@(args => ToggleManualEntry())"
                                                  Disabled="@(ActionSaveScan.Busy
                                                      || ScanState == ToggleState.Missing)" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                class="scan-action-label rz-m-0">

                                        Manual

                                    </RadzenText>

                                </RadzenStack>


                                @* SAVE *@

                                <RadzenStack Gap="5px"
                                             Orientation="Orientation.Vertical"
                                             AlignItems="AlignItems.Center"
                                             class="scan-action-item">

                                    <RadzenButton class="rz-border-radius-5"
                                                  Icon="@(ActionSaveScan.Busy
                                                      ? "schedule"
                                                      : "save")"
                                                  ButtonStyle="ButtonStyle.Success"
                                                  Size="ButtonSize.Small"
                                                  Click="@(async () => await SaveScan())"
                                                  Disabled="@(SaveBtnDisabled
                                                      || ActionSaveScan.Busy)" />

                                    <RadzenText TextStyle="TextStyle.Caption"
                                                class="scan-action-label rz-m-0">

                                        Save

                                    </RadzenText>

                                </RadzenStack>

                            </RadzenStack>
                        }

                    </RadzenStack>

                </RadzenCard>

            </RadzenStack>

        </ChildContent>

    </Loading>

    <SavingLoading IsVisible="ActionSaveScan.Busy"
                   Text="Saving scanned items..." />

</RadzenStack>
